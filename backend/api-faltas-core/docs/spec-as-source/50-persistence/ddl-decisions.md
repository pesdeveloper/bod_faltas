# Decisiones DDL — registro único de `DECISION_DDL-*`

> **Estado documental:** PRE_DDL_PLAN
> **Autoridad DDL:** SUPPORTING
> Este documento es el registro único de decisiones físicas pendientes y
> cerradas identificadas con prefijo `DECISION_DDL-`. Ninguna decisión listada
> aquí reabre un contrato funcional, un evento, un estado o una transición: son
> exclusivamente de diseño físico/DDL y no bloquean `READY_FOR_DDL` (ver
> `../00-governance/ready-for-ddl-gate.md`). El inventario de tablas al que
> refieren estas decisiones vive en `mariadb-logical-model.md`; los deltas
> transversales de infraestructura viven en `inmemory-mariadb-deltas.md`.

## Pendientes

> **R3 — Nuevas entradas (2026-07-17):** `DECISION_DDL-SNAP-01`, `DECISION_DDL-SNAP-02`, `DECISION_DDL-EVID-01`, `DECISION_DDL-PAGO-03`, `DECISION_DDL-FORMA-01`, `DECISION_DDL-PLAN-01`, `DECISION_DDL-MOV-01` a `DECISION_DDL-MOV-04`, `DECISION_DDL-ECPR-01` — todas descubiertas durante la reconciliación triple Java ↔ histórico 2026-06-23 ↔ spec vigente.

| ID | Tema | Alternativas | Criterio de cierre | Fuente normativa |
|---|---|---|---|---|
| `DECISION_DDL-RF-005` | Tratamiento físico/migratorio del valor legacy `FALLO_CONDENATORIO_PAGADO` (código 5) de `ResultadoFinalActa` | (a) migrar datos históricos al valor canónico `CONDENA_FIRME_PAGADA`; (b) conservar en catálogo inactivo/legacy solo de lectura; (c) admitir temporalmente solo durante ventana de migración | Se cierra al aprobar el DDL de `fal_acta.resultado_final` y la estrategia de migración de datos históricos, si existieran | `../10-domain/states-events-catalogs.md`, `../00-governance/glossary.md` |
| `DECISION_DDL-SNAP-01` | `fal_acta_snapshot`: OCC (`version_row`) y `cod_bandeja VARCHAR` longitud | OCC: (a) agregar `version_row INT NOT NULL DEFAULT 0` antes del adapter JDBC (coherente con otras tablas mutables); (b) no OCC — snapshot es regenerable desde evento, se puede reconstruir sin conflicto. Longitud `cod_bandeja`: (a) `VARCHAR(50)` (actual en spec); (b) ajustar según catálogo `CodigoBandeja` máximo confirmado | Se cierra al diseñar el adapter JDBC de `ActaSnapshotRepository`; verificar longitud máxima real de `CodigoBandeja` | `mariadb-logical-model.md` (sección Snapshot), `inmemory-mariadb-deltas.md` |
| `DECISION_DDL-SNAP-02` | `fal_acta_snapshot`: campos presentes en histórico 2026-06-23 pero ausentes en `FalActaSnapshot.java` vigente | Campos a evaluar: `fh_acta`, `nro_acta`, `tipo_acta`, `id_dep`/`ver_dep`, `id_insp`/`ver_insp`, `id_persona_infractor`, `nombre_infractor`, `doc_infractor_txt`, textos/domicilio/ubicación del hecho, `esta_cerrada`, `si_visible_bandeja`, `prioridad`, bloqueantes separados (`si_bloqueante_med_prev`, `si_bloqueante_rodado`, `si_bloqueante_doc_ret`), gestión externa (`si_gestion_ext`, `gestion_externa_activa_id`, `tipo_gestion_ext`, `estado_gestion_ext`, `resultado_gestion_ext`), fechas de vencimiento (`fh_vto_presentacion`, `fh_vto_apelacion`, `fh_vto_apremio`), `id_evt_ult`, `id_notif_ult`, `fh_snapshot`, `rebuild_id`, `fh_ult_mod`, `id_user_ult_mod`. Alternativas: (a) agregar los campos ausentes que tengan uso operativo confirmado (bandejas, portales, filtros); (b) excluir los que el dominio actual no use; (c) incluir todos como desnormalización de consulta rápida | Se cierra al definir el DDL completo de snapshot con uso operativo comprobado para cada campo; confirmar cuáles sirven para filtros o UX actuales | `mariadb-logical-model.md` (sección Snapshot), `../30-projections/snapshot-bandejas-acciones.md` |
| `DECISION_DDL-EVID-01` | `fal_acta_evidencia`: campos en histórico 2026-06-23 ausentes en `FalActaEvidencia.java` vigente | Campos a evaluar: `nombre_archivo VARCHAR(120)`, `mime_type SMALLINT`, `hash_evid VARCHAR(128)`, `orden_evid SMALLINT`, `fh_captura DATETIME(6)` (¿corresponde a `fechaRegistro` de Java?), `fh_alta DATETIME(6)`, `id_user_alta CHAR(36)`. Alternativas: (a) agregar todos — trazabilidad y control de archivos; (b) incluir solo `fh_alta`/`id_user_alta` mínimos de auditoría; (c) mantener mínimo Java (5 campos) y enriquecer en DDL según uso operativo confirmado | Se cierra al definir el DDL de `fal_acta_evidencia`; aclarar si `fechaRegistro` Java corresponde a `fh_captura` del histórico o es campo nuevo | `mariadb-logical-model.md` (sección Snapshot y Evidencia), `../30-projections/snapshot-bandejas-acciones.md` |
| `DECISION_DDL-PAGO-03` | `fal_acta_obligacion_pago`: campos en histórico 2026-06-23 ausentes en `FalActaObligacionPago.java` vigente | Campos: `origen_obligacion SMALLINT NOT NULL` (catálogo FALTAS/APREMIO/JUZGADO) y `obligacion_reemplazada_id BIGINT NULL` (FK self). Alternativas: (a) agregar ambos — trazabilidad de origen e historial de reemplazo; (b) excluir — el dominio actual no los usa funcionalmente; (c) agregar solo `origen_obligacion` como campo de clasificación | Se cierra al diseñar el adapter JDBC de `ObligacionPagoRepository`; confirmar si Java los necesita para funcionalidad operativa o son solo trazabilidad histórica | `mariadb-logical-model.md` (sección Pagos, `fal_acta_obligacion_pago`) |
| `DECISION_DDL-FORMA-01` | `fal_acta_forma_pago`: `fh_vencimiento DATETIME(6) NULL` presente en histórico 2026-06-23, ausente en `FalActaFormaPago.java` | Alternativas: (a) agregar `fh_vencimiento` — permite registrar vencimiento del recibo sin rechazar pagos fuera de término (clasificación `tipo_vencimiento_pago`); (b) excluir — el dominio actual no valida ni rechaza pagos por vencimiento | Se cierra al diseñar el adapter JDBC de `ObligacionPagoRepository`; confirmar si Java necesita este campo para funcionalidad operativa | `mariadb-logical-model.md` (sección Pagos, `fal_acta_forma_pago`) |
| `DECISION_DDL-PLAN-01` | `fal_acta_plan_pago_ref`: `fh_ultimo_pago` y `fh_ult_sync_ingresos` presentes en `FalActaPlanPagoRef.java`, ausentes en histórico 2026-06-23 | Alternativas: (a) incluir ambos en DDL — están en Java vigente y son útiles para UX de mora/seguimiento; (b) incluir solo `fh_ult_sync_ingresos` como watermark de sincronización; (c) decisión diferida | Java vigente los tiene → recomendación: incluir en DDL. Cerrar al validar uso funcional y confirmar que no hay código que espere el campo | `mariadb-logical-model.md` (sección Pagos, `fal_acta_plan_pago_ref`) |
| `DECISION_DDL-MOV-01` | `fal_acta_pago_movimiento`: `tipo_vencimiento_pago SMALLINT NULL` presente en histórico 2026-06-23, ausente en Java | Alternativas: (a) incluir — permite clasificar si el pago fue dentro o fuera de término (informativo); (b) excluir — Java no lo usa, no hay enum definido en código vigente | Se cierra al diseñar el adapter JDBC; verificar si existe `TipoVencimientoPago` enum en Java o si solo era un catálogo del histórico | `mariadb-logical-model.md` (sección Pagos, `fal_acta_pago_movimiento`) |
| `DECISION_DDL-MOV-02` | `fal_acta_pago_movimiento`: `fh_recepcion_tecnica DATETIME(6) NULL` presente en histórico 2026-06-23, ausente en Java | Alternativas: (a) incluir — permite distinguir fecha funcional del hecho (`fh_movimiento`) de la fecha de recepción técnica en Faltas; (b) excluir — Java no lo usa; `fh_alta` ya registra cuándo se persistió la fila | Se cierra al diseñar el adapter JDBC; confirmar si el caso de uso de latencia/auditoría entre evento externo y persistencia local requiere este campo | `mariadb-logical-model.md` (sección Pagos, `fal_acta_pago_movimiento`) |
| `DECISION_DDL-MOV-03` | `fal_acta_pago_movimiento`: longitud de `motivo_aplicacion_pago_anterior VARCHAR(?)` | Java: `String` sin anotación de longitud; no hay validación de máximo en el Builder. Alternativas: (a) `VARCHAR(255)` — coherente con `referencia_externa` y campos de motivo similares; (b) `VARCHAR(500)` — capacidad para motivos largos; (c) `TEXT` — sin límite, pero rompe consistencia del modelo | Se cierra al definir el DDL de `fal_acta_pago_movimiento`; si se agrega validación de máximo al Builder Java, ese valor es autoritativo | `mariadb-logical-model.md` (sección Pagos, `fal_acta_pago_movimiento`) |
| `DECISION_DDL-MOV-04` | `fal_acta_pago_movimiento`: catálogo `ClasificacionPago` difiere entre histórico 2026-06-23 y Java vigente | Histórico: NORMAL=1/OBLIGACION_ANTERIOR=2/DUPLICADO_REAL=3 (3 valores). Java (`ClasificacionPago.java`): NORMAL=1/DUPLICADO_REAL=2/EXCEDENTE=3/OBLIGACION_ANTERIOR=4 (4 valores, códigos distintos). Java es autoritativo (producción vigente). Implicación DDL: `clasificacion_pago SMALLINT NOT NULL DEFAULT 1`; catálogo incluye `EXCEDENTE=3` y `OBLIGACION_ANTERIOR=4`. Acción: confirmar con equipo que ningún dato histórico en producción fue persisitido con los códigos del histórico 2026-06-23 | Se cierra al confirmar que no hay datos pre-existentes con el catálogo histórico; documentar como decisión resuelta | `mariadb-logical-model.md` (sección Pagos, `fal_acta_pago_movimiento`), `ClasificacionPago.java` |
| `DECISION_DDL-ECPR-01` | `fal_acta_economia_proyeccion`: `fh_corte_economico` y `fh_ult_mod` — nullabilidad difiere entre histórico (NOT NULL) y Java (nullable) | Histórico 2026-06-23: ambos `NOT NULL`. Java: `fhCorteEconomico` y `fhUltMod` son `LocalDateTime` sin validación de no-nulo (inicializados `null` en el constructor). Alternativas: (a) NOT NULL con DEFAULT `NOW()` — coherente con histórico; garantiza que el campo siempre está informado al persistir; (b) nullable — coherente con Java; permite registros sin corte definido hasta la primera actualización | Se cierra al diseñar el adapter JDBC de `EconomiaProyeccionRepository`; confirmar si se garantiza que ambos campos estén poblados antes de persistir en contexto JDBC | `mariadb-logical-model.md` (sección Pagos, `fal_acta_economia_proyeccion`) |
| `DECISION_DDL-DOC-01` | `version_row` en `fal_documento` (OCC físico para numeración y estados documentales) | (a) agregar `versionRow` a `FalDocumento` antes del adapter JDBC; (b) confiar únicamente en `SEQUENCE` + transacción sin OCC adicional | Se cierra al definir el mecanismo de concurrencia del adapter JDBC para `DocumentoService` | `inmemory-mariadb-deltas.md` (sección Concurrencia), `../20-application/command-contracts.md` |
| `DECISION_DDL-NOTI-01` | `version_row` en `fal_notificacion` | (a) agregar `versionRow`; (b) delegar exclusivamente en `ResultadoPositivoInMemoryMonitor`/equivalente transaccional | Se cierra al definir el adapter JDBC de `NotificacionService` | `inmemory-mariadb-deltas.md` (sección Concurrencia), `../20-application/fallo-command-contracts.md` (CMD-FALLO-004) |
| `DECISION_DDL-GEXT-01` | `version_row` en `fal_acta_gestion_externa` | (a) agregar `versionRow`; (b) unicidad `si_activa=true` por acta como único mecanismo | Se cierra al definir el adapter JDBC de gestión externa | `inmemory-mariadb-deltas.md` (secciones Selección de registro activo/vigente y Concurrencia), `../20-application/command-contracts.md` |
| `DECISION_DDL-BLOQ-01` | `version_row` en `fal_acta_bloqueante_cierre_material` | (a) agregar `versionRow`; (b) mantener exclusión solo por invariante de repositorio | Se cierra al definir el adapter JDBC de bloqueantes materiales | `inmemory-mariadb-deltas.md` (sección Concurrencia), `../20-application/command-contracts.md` |
| `DECISION_DDL-PERS-01` | Unicidad física de `fal_persona` sobre `(tipo_documento, nro_documento)` para personas identificadas | (a) `UNIQUE` simple; (b) `UNIQUE` parcial/condicional (`si_identificado=true`) según soporte del motor | Se cierra al elegir motor/versión MariaDB definitiva y su soporte de índices únicos parciales | `inmemory-mariadb-deltas.md` (sección Unicidades) |
| `DECISION_DDL-PAGO-01` | Identidad física de `FalPagoVoluntario`/`FalPagoCondena` (actualmente `String`, vistas sobre `fal_acta_obligacion_pago`) | (a) mantener como vistas de aplicación sin tabla propia; (b) exponer directamente `FalActaObligacionPago` en la capa de aplicación | Se cierra al diseñar la capa de acceso a datos de pagos en el adapter JDBC | `mariadb-logical-model.md` (sección Pagos), decisión D2 |
| `DECISION_DDL-PAGO-MOV-01` | `fal_acta_pago_movimiento`: clave de negocio de recibo real (`origen_movimiento+cmte_pg+pref_pg+nro_pg`, obligatoria y completa en todo `PAGO_CONFIRMADO` original — `movimiento_origen_id IS NULL` —, nulable solo en el movimiento de aplicación creado por `PAGRES` u otros movimientos internos sin recibo), unicidad de aplicación por `movimiento_origen_id` para el caso específico "a lo sumo un movimiento de aplicación (`PAGO_CONFIRMADO`/`NORMAL`) por `PAGANT` original", columna `motivo_aplicacion_pago_anterior`, FK de `movimiento_origen_id` hacia la propia tabla, y OCC/transacción del adapter | Para la terna de recibo: (a) `CHECK` de terna completa-o-totalmente-nula (`(cmte_pg IS NULL AND pref_pg IS NULL AND nro_pg IS NULL) OR (cmte_pg IS NOT NULL AND pref_pg IS NOT NULL AND nro_pg IS NOT NULL)`); (b) `CHECK`/regla aplicativa que exija terna no nula cuando `movimiento_origen_id IS NULL AND tipo_movimiento='PAGO_CONFIRMADO'` (recibo obligatorio para todo pago original real; motor SQL puede no soportar subquery en `CHECK`, en cuyo caso la regla queda exclusivamente aplicativa, replicando la validación ya presente en `PagoMovimientoService.registrar`); (c) `UNIQUE` compuesto simple sobre `(origen_movimiento, cmte_pg, pref_pg, nro_pg)` cuando la terna es no nula (parcial/condicional según soporte del motor), verificado de forma atómica junto con el insert (equivalente físico del bloque `synchronized` de `InMemoryPagoMovimientoRepository.append`). Para `movimiento_origen_id`: (d) `UNIQUE` parcial/condicional `WHERE movimiento_origen_id IS NOT NULL AND tipo_movimiento='PAGO_CONFIRMADO' AND clasificacion_pago='NORMAL'` (evita colisionar con el uso no relacionado de `movimiento_origen_id` para enlazar un reverso con su `PAGO_CONFIRMADO` original); (e) `FOREIGN KEY (movimiento_origen_id) REFERENCES fal_acta_pago_movimiento(id)`, autorreferencial; (f) verificación exclusivamente aplicativa (sin `UNIQUE` físico) como alternativa a (d), replicando el patrón `synchronized` de InMemory con OCC de la obligación vigente. Columna nueva: `motivo_aplicacion_pago_anterior VARCHAR` (o equivalente), `NULL` por defecto, poblada únicamente en el movimiento de aplicación (`PAGRES`); es la fuente de verdad de idempotencia de motivo, nunca derivada de `fal_acta_evento.descripcion_legible`. Nota: el reverso y la resolución comparten hoy la misma columna `movimiento_origen_id` con semántica distinta según `tipo_movimiento`; ningún caso conocido de dominio reversiona un `PAGANT` ya resuelto (o viceversa), pero esa exclusión mutua no está modelada como invariante explícita fuera de este DDL | Se cierra al diseñar el adapter JDBC de `PagoMovimientoService`/`ResolverPagoObligacionAnteriorService`, según soporte de índices/`CHECK` parciales del motor/versión MariaDB elegida | `mariadb-logical-model.md` (sección "Pagos", `fal_acta_pago_movimiento`), `inmemory-mariadb-deltas.md` (sección Unicidades), `../20-application/command-contracts.md` |
| `DECISION_DDL-ENUM-01` | Representación física de enums persistibles sin `codigo()` (categoría `NO_EXPLICIT_CODE`): como mínimo `EstadoFalloActa`, `EstadoApelacionActa`, `EstadoPagoCondena`, `TipoDiaNoComputable`, `OrigenDiaNoComputable` | (a) agregar código estable explícito en Java antes del adapter JDBC y usar `SMALLINT`; (b) aprobar código `String` estable y usar `CHAR`/`VARCHAR` con constraint | Se cierra con: inventario completo de enums persistibles sin `codigo()`; decisión aprobada por enum o política global; actualización del modelo Java si corresponde; DDL consistente con esa decisión. Prohibido `ordinal()`, inferir números por posición, o persistir `name()` sin esta decisión | `inmemory-mariadb-deltas.md` (sección Enums y catálogos), `jdbc-strategy.md` (sección de enums), `mariadb-logical-model.md` (sección 3) |

## Cerradas

Ver `mariadb-logical-model.md`, sección 4 ("Decisiones físicas ya cerradas",
D1–D10, P1–P2). No se repite el detalle aquí para evitar dos fuentes del mismo
dato; este documento es exclusivamente el registro de decisiones con prefijo
`DECISION_DDL-`.

## Regla de gobierno de este registro

- Todo nuevo `DECISION_DDL-*` se agrega únicamente a este archivo.
- Ninguna decisión aquí puede alterar un estado, evento, transición, comando o
  contrato HTTP ya cerrado en `10-domain/`, `20-application/`, `30-projections/`
  o `40-api/`. Si una decisión física parece requerir ese cambio, se detiene y
  se reporta como gap, no se resuelve por inferencia.
- Al cerrar una decisión, se mueve de "Pendientes" a una entrada referenciada
  en `mariadb-logical-model.md` sección 4, con el mismo ID para trazabilidad.
