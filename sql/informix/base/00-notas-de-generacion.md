# Notas de generación

Este paquete fue regenerado con las siguientes reglas vigentes:

- `Fal*` para dominio propio de faltas
- `Num*` para numeración / talonarios transversal
- `Stor*` para storage documental transversal
- conservación de `RubroCom` y `RubroComVersion` como entidades compartidas existentes
- máximo interno de `30` caracteres para nombres de índices, constraints y secuencias

Observaciones:

- se regeneraron nombres de secuencias a aliases cortos
- se regeneraron nombres de PK, FK, UQ e índices con formato compacto
- conviene validar el paquete en una ejecución real sobre Informix 12.10 antes de congelarlo
